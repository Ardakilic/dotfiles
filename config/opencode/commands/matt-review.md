---
description: Two-axis code review (Standards + Spec) of the diff since a fixed point — from mattpocock/skills
---

Load the `matt-review` skill and follow its process.

**Input**: Optionally specify a fixed point (commit, branch, tag, e.g. `/matt-review main`). If omitted, ask the user.
